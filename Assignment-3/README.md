## Code structure
All code is in layers.py

First comes the functions, then code for parsing file and storing as dictionary

Then testing all functions for the 2 inputs for each function, it prints the generated output, then prints the given output from the text file.

Some comments are given as to how the forward/backward propagation is done for functions that needed some explanation.